meta {
  name: Refresh
  type: http
  seq: 4
}

post {
  url: {{base_url}}/auth/refresh
  body: json
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "refresh_token": "85plqk...1abV"
  }
}

assert {
  res.status: eq 422
  res.body.message: isDefined
  res.body.errors.refresh_token: isDefined
}
